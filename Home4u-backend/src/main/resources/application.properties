# ?????? ??
spring.application.name=Home4U

# ?? ?? ??
server.port=8080

# ?? MySQL ?????? ??
spring.datasource.url=jdbc:mysql://localhost:3306/home4u?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=rjsgh!@#8079
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? Hibernate (JPA) ??
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ? HikariCP (??? ?) ??
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000

# ? JWT ?? (?? ?? ?? ??? ??)
jwt.secret=${JWT_SECRET}  # ? ?? ??? ??
jwt.expiration=86400000  # 1? (24??)

# ? OAuth ?? (??? API)
naver.client.id=q7higb27zy
naver.client.secret=jAafyv6bc82doASJaBDHa42FYZG8NQTis2So5miC

# ? CORS ?? (?? ??? ?? ??, ?? ????? ?? ???? ?? ??)
cors.allowed-origins=*

# ? ?? ?? (DEBUG ?? ??? ??)
logging.level.org.springframework=INFO

# ? ?? ??? ??
spring.jackson.time-zone=Asia.Seoul

openai.api.key=YOUR_OPENAI_API_KEY
